<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.teshub_v1">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Permiso para acceder a imágenes en la galería (Android 13+) -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TeshubV1"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="31">

        <!-- Clave de API para Google Maps -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyCJeDKp2dLQMoO-Otx9hYMLDtQEzsB7y6s" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".HomeContainerActivity"
            android:exported="false" />

        <activity
            android:name=".CrearPublicacionActivity"
            android:exported="false" />

        <activity
            android:name=".RegisterActivity"
            android:exported="false" />

        <activity
            android:name=".ForgotpasswordActivity"
            android:exported="false" />

        <activity
            android:name=".ResetpasswordActivity"
            android:exported="false" />

        <!-- Actividades del módulo de Eventos -->
        <activity android:name=".ui.eventos.EventoDetalleActivity" />
        <activity android:name=".ui.eventos.CrearEventoActivity" />
        <activity android:name=".ui.eventos.SeleccionarUbicacionActivity" />
        <activity android:name=".ui.eventos.EditarEventoActivity" />

    </application>

</manifest>